name: Demo GitHub Actions Events
run-name: wwwwwwwwwwwwwwww

on:
  # Webhook
  push:
    branches:
      - main
      - master
      - demo-*
  pull_request:
    branches:
      - main
      - master
  issues:
    types:
      - opened
      - closed

  workflow_dispatch:
    inputs:
      param1:
        description: Provide param1
        required: true
        default: default_param1

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
#      - name: step1
#        run: echo "This is job1 step1"
#      - name: inputs1
#        run: echo param1 ${{ inputs.param1}}
#      - name: input2
#        run: echo "param2 ${{ inputs.param2}}"
#      - name: input3
#        run: echo 'param3 ${{ inputs.param3}}'
      - name: conditional1
        run: echo "param is CAT"
        if: ${{ inputs.param1 == "cat" }}
      - name: conditional2
        run: echo "param is DOG"
        if: ${{ inputs.param1 == "dog" }}
